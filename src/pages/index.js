import React from 'react';

import Form from '../components/Form.js';

const Home = () => {
    return (
        <React.Fragment>
            <Form />
        </React.Fragment>
    );
};

export default Home;
